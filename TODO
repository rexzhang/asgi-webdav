进行中:
    ☐ Fix MemoryProvider with macOS finder(create new file)
    ☐ Test big(1GB+) file in MemoryProvider
    ☐ 配置文件现代化
        保存对 JSON 格式的支持
        TOML 作为配置文件的第一格式
        ✔ pydantic => dataclass @done
        ✔ 添加 TOML 支持 @done
        ✔ docker 环境支持自动尝试多种配置文件后缀 @done(25-10-31)
            ✔ 默认配置文件改为 webdav.toml @done(25-10-31)
        ☐ config.config 这个模块成员使用 lazy load @high
        ☐ 自动尝试多种配置文件后缀这个行为通过命令行打开,默认不开启
        ☐ 通过配置文件实现 dev 模式的切换
        ☐ 通过配置文件实现 litmus 模式的切换 @wip
        ☐ 更新文档 @wip
        ☐ 使用 TOML 方便添加注释的特性, 增加各种使用场景配置范例 @long-term
        ☐ 以 TOML 下的可读性为标准,适当调整配置文件结构 @long-term
    ☐ 重构 DAVResponse
        ☐ 统一 401 的响应行为 @high @next-release
        ☐ 浏览器请求才弹认证请求框, 服务器返回 401 时不弹认证框
        ☐ 重构部分 DAVResponse 代码, 这个与压缩依赖部分一起修改
        ☐ 顺带解决 litmus 的一个告警?
            有一个 DELETE 方法不能使用 Transfer-Encoding: chunked
            需要 _send_in_one_body()
        ✔ 移除 brotli 压缩方法 @done(25-11-14)
        ✔ 添加sztd支持 @done(25-11-14)
    ☐ 浏览器后台(内部)界面 @low
        ☐ info界面
            ☐ /_/admin/INDEX
            ☐ /_/admin/config
        ☐ 当前配置信息,JSON格式
            ☐ 有无配置文件
            ☐ 环境变量
            ☐ 最终配置
            ☐ 自动隐藏密码信息
        ☐ 协程信息
            目标为方便及时查看方便性能优化
            ☐ 主要是读大文件的问题
        ☐ 协助生成密码字段工具
            /_/admin/gpw

长期性任务:
    ☐ 更好的类型标注
        - 主要解决类型推断问题, 提高对静态类型检查工具的支持
        - 在维护代码时顺便解决相关代码出现的类型推断错误
        ☐ 终极目标:通过 mypy 检查
        ☐ 消除 DAVRequest 相关的类型推断错误
        ☐ 消除 DAVResponse 相关的类型推断错误
    - 解耦/分离可作为第三方库的核心代码和实现应用的代码
        降低耦合度, 让其他项目能更便捷的添加 webdav 能力
        终极目标为: 实现将核心代码剥离为一个独立的库
        - 核心代码作为 ASGI app 分发
    - 文档问题
        ☐ 规范文档结构
        ☐ 文档自动部署失效问题
    - 每年定期更新上游依赖
        ☐ aiofiles
        ✔ xmltodict v1.0.2 @done(25-12-09)

待实现:
    ☐ 基于配置,检查依赖安装情况,以便在启动时告警
    ✔ 兼容 Py3.14  dataclass-wizard 不兼容 py3.14 @done(25-12-14)
    ☐ 浏览器前台界面 @low
        ☐ 更干净的目录过滤机制
        ☐ 更好看的界面
            ☐ HTMLX
            ☐ 时区支持,自动使用浏览器时区
            ☐ size自动单位
            ☐ 可基于文件名/大小/时间/正反排序
        ☐ 文件上传
        ☐ 登出

待实现(低优先级):
    ☐ Digest auth support neon @deplay 需要时间过多
        WinSCP Digest 兼容问题, WinSCP,版本5.17.10(构建版本11087)OS:windows 10
    ☐ 共享链接
        ☐ `/_/share/ + random string`
            只读?
        ☐ /_/share/manage|list
            ☐ 列出当前账号有效的share
        ☐ /_/share/api
        ☐ 只读???
        ☐ 有效期
        ☐ 创建人/owner
        ☐ 路径/path
        ☐ 最大的问题应该是provider支持问题,如何重定向到provider内的某个API来实现
            ☐ owner权限检查
            ☐ 文件是否存在
            ☐ 文件获取

待验证 bug:
    ☐ fileball连接会导致500错误
    ☐ 断点续传支持开始和结尾，而不是只支持开始
    ☐ 每次切换目录时在递归遍历目录下的所有文件
        0.3.1 以及以前没有这个问题
        当请求者不是浏览器的时候不遍历目录下所有文件
        根据 agent 判断来源
        TextFileCharsetDetect 默认不打开
        如果客户端没有请求文件详细信息，不逐个文件检查 GuessTypeExtension
    ☐ 符号链接会导致错误

待确定可行性/实现方案:
    ☐ 统一 user/account 的概念 @low
        当前 user/account 的概念是混合的, 在代码中也是
        需要做出明确的定义和规则; 统一为一个名称?
    ☐ 完善的 LDAP 支持 @high
    ☐ display server info in page `/_/admin` or `/_/` @high
    ☐ Fail2ban(docker) @critical
    ☐ NFSProvider
    ☐ SQL database provider
    ☐ 支持 docker health check @high, 参考 watchtowe
    ☐ 性能优化
        ☐ 优化默认扫描深度
            PROPFIND
        ☐ 优化 GET 对超大文件读取性能的问题(通过Finder/IINA)
        ☐ rewrite MemoryProvider with mmap or other
